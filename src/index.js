import React from 'react';
import ReactDOM from 'react-dom';
import PoliciesApp from './PoliciesApp';
import './styles.css';

ReactDOM.render(
  <React.StrictMode>
    <PoliciesApp />
  </React.StrictMode>,
  document.getElementById('root')
);

